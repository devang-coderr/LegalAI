import { StaticPageShell } from "@/components/layout/StaticPageShell";

export default function PrivacyPage() {
  return (
    <StaticPageShell title="Privacy Policy">
      <p>
        This is a frontend prototype. No real user data is collected, stored, or
        processed by this build — form submissions use mock services only.
      </p>
      <p>
        A complete privacy policy will be published here once LegalAI&apos;s backend,
        authentication, and data-handling systems are live.
      </p>
    </StaticPageShell>
  );
}
